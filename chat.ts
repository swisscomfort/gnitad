import { Router } from 'express';
const router = Router();
// TODO: Implement chat routes
router.get('/:matchId', (req, res) => res.status(501).json({ error: 'Not implemented yet' }));
export default router;
