import { Router } from 'express';
const router = Router();
// TODO: Implement taxonomy routes
router.get('/', (req, res) => res.status(501).json({ error: 'Not implemented yet' }));
export default router;
