import { Router } from 'express';
const router = Router();
// TODO: Implement matching routes
router.get('/feed', (req, res) => res.status(501).json({ error: 'Not implemented yet' }));
export default router;
