import { Router } from 'express';

const router = Router();

// Placeholder - implement routes here
router.get('/', (req, res) => {
  res.json({ message: 'User routes - to be implemented' });
});

export default router;
